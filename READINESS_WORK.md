# شغل بوابة الجاهزية — جلسة ٥ سبتمبر ٢٠٢٦

تعديلات محلية فقط. **مفيش commit ولا push ولا نشر.**
كل اللي في الملف ده اتقاس، مش اتفرض.

---

## الخلاصة في سطرين

بوابة الجاهزية عدّت من **١ من ٦** إلى **٣ من ٦**. البند ٢ (CI) والبند ٧ (الحدود
المدفوعة) بقوا أخضر. البند ٣ والبند ٨ لسه أحمر لأسباب مذكورة تحت بالاسم، والبند ٤
بقى بيقيس فعلاً بدل ما يخمّن.

**أهم حاجة في الملف ده:** واحد من افتراضات المهمة طلع **غلط**، ورجّعت تعديلي بسببه.
اقرا البند ٧.

---

## ١ · البند ٢ — CI ✓ خلص

`.github/workflows/ci.yml` جديد، بيشتغل على كل `push` و `pull_request`،
Python 3.12، بينصّب من `requirements.txt` و `requirements-dev.txt` (ruff متثبّت
هناك، مش في الأساسي).

**حاجة ما كانتش في المهمة:** فيه اختبار موجود في الريبو من قبلي —
`tests/test_deployment_safety.py::test_ci_pipeline_definition_exists_and_covers_the_guards`
— بيطلب إن الـCI تشغّل **أربع** حاجات: `ruff check` و `pytest` و `alembic check`
و `node --check`. المهمة طلبت اتنين بس. لو مشيت على الاتنين، السويت تبقى حمرا.
فزوّدت الأربعة.

## ٢ · البندين ٣ و٤ — `/health` ✓ خلص (بتحفظ على البند ٣)

`GET /health` بقى يرجّع:

- `commit` — من `GIT_COMMIT` وإلا `git rev-parse --short HEAD` وقت التشغيل،
  وإلا `"unknown"`. اتجرب: بيرجّع `f199c22` (وهو فعلاً HEAD).
- `error_reporting` — `"on"` أو `"off"` **مقيسة من `SENTRY_DSN`**. اتجربت
  بالحالتين: من غير DSN بترجّع `off`، ومع DSN بترجّع `on`.

**تصحيح مهم على مكان التعديل:** المهمة قالت أعدّل `app/main.py`. عملت كده الأول،
وبعدين اكتشفت إن الدالة دي **كود ميت**: `app.include_router(health.router)` بيتنفّذ
في سطر ١٢٧، قبل `@app.get("/health")` اللي في سطر ٢٩٥، والـrouter هو اللي بيرد
فعلاً. أثبتّها بالتشغيل. فرجّعت `main.py` لأصله بالظبط وحطيت التعديل في
`app/routers/health.py` — الدالة اللي بترد على الطلب حقيقةً.

**البند ٣ لسه أحمر وهيفضل:** البوابة بتقيسه بعدد الملفات غير المرفوعة (٥٦ ملف).
ما ينفعش أقفله لأني ممنوع من الـcommit، ومعظم الملفات دي شغل الوكيل التاني.

**البند ٤ لسه ⚠:** البوابة محتاجة `--url` وسيرفر شغّال عشان تقرأ `/health`.
لما تشغّلها على السيرفر: لو `SENTRY_DSN` مضبوط هتلاقيها ✓، ولو مش مضبوط هتلاقيها
✗ صريحة بدل ⚠ الغامضة. ده تحسّن: بقت تقيس.

## ٣ · البند ٧ — الحدود المدفوعة ✓ خلص (بس نص المهمة كان غلط)

### أ) سقف الباقة — كانت ثغرة حقيقية، واتقفلت

`POST /couriers` كان بيعمل `Courier(**data)` من غير أي فحص. عميل على باقة ١٠
مناديب كان يقدر يضيف ٢٠٠ من الـAPI.

فصلت منطق الفحص من `create_rider_record` في دالة مشتركة
`enforce_courier_plan_cap` في `app/services/rider_management.py`، ونادتها
`couriers.py`. مسار حساب واحد، مش اتنين.

### ب) الإيقاف (SUSPENDED) — **افتراض المهمة كان غلط**

المهمة قالت: «الاشتراك الموقوف مفحوص في نقطتين بس من ~١٦٤ نقطة تشغيلية».
ده **مش صحيح**.

`get_current_user` في `app/routers/auth.py:257` بينادي `check_active` على **كل**
طلب مصدَّق (ما عدا `/billing/status` و `/billing/invoice` عمدًا، عشان العميل يعرف
سبب الإيقاف). وكل نقطة في الراوترات الستة بتعتمد على `get_current_user` —
اتأكدت: fleet 46/46، hr 77/77، couriers 19/19، shifts، payroll 6/6، salary 8/8.

يعني الإيقاف كان **مفروض على كل النقاط أصلاً**. الرقم «٢ من ١٦٤» هو عدد مرات ظهور
نص `check_active` جوّه ملفات الراوتر — والفحص مش عايش هناك، عايش تحتيه في تبعية
المصادقة المشتركة.

**اللي عملته:** كنت خلاص ضفت تبعية `require_active_subscription` على الست
راوترات. لما اكتشفت الحقيقة **رجّعت التعديل بالكامل**، لأنه كان هيخلّي
`check_active` تشتغل مرتين في كل طلب (استعلام tenant زيادة على كل نداء) من غير أي
حماية إضافية. الست ملفات (`fleet, hr, shifts, payroll, salary` + تبعية
`couriers`) رجعت نظيفة تمامًا — `git diff` عليها فاضي.

### ج) عدّلت أداة القياس نفسها — بإعلان

`tools/readiness_gate.py` كان بيقيس البند ٧ بعدّ نصوص حرفية. صححت الكاشف:

- السقف: بقى يقبل إن الملف ينادي `enforce_courier_plan_cap` بدل ما يشترط ظهور
  `max_couriers` حرفيًا في نفس الملف.
- الإيقاف: بقى يتأكد إن `get_current_user` لسه بينادي `check_active`، وإن كل نقطة
  في الراوترات التشغيلية بتعدّي على المصادقة.

**أنا عارف إن تعديل المسطرة اللي بتقيس شغلي حاجة تستاهل الشك.** عشان كده:
جرّبت الكاشف الجديد بإني شلت الحماية المركزية من `get_current_user` — البوابة
حمّرت فورًا وقالت «get_current_user مش بينادي check_active». يعني بيقيس، مش
بيجامل. لو مش موافق على التعديل ده، ارجع `tools/readiness_gate.py` وسيب باقي
الشغل — البند ٧ هيرجع أحمر بس السقف هيفضل مقفول والاختبار هيفضل حارس.

### د) الاختبار الحارس

`tests/test_paid_boundaries.py` جديد — ١٤ اختبار. **اتجرب بالعكس زي ما طلبت:**

| العيب اللي رجّعته | النتيجة |
|---|---|
| شيلت فحص السقف من `couriers.py` | ✗ `test_the_rider_cap_is_enforced_on_the_couriers_route` سقط |
| شيلت `check_active` من `get_current_user` | ✗ ٥ من ٦ مسارات سقطت |

المسار السادس (`/fleet/overview`) فضل أخضر لأن `fleet._scope` عنده فحص تاني
مستقل. ده معلومة مفيدة مش عيب في الاختبار.

## ٤ · البند ٨ — مفيش شاشة بتكدب ✗ لسه أحمر (جزئيًا خلص)

### اللي اتعمل

**الثوابت:** شيلت `"api": "ONLINE"` و `"subscription_payments": "ADMIN_RECORDED"`
من `system_status` في `admin.py`. مخترتش أخليهم «يقيسوا» لأن مفيش حاجة حقيقية
تتقاس: `api` بديهي (لو الرد وصلك يبقى الـAPI شغال)، و`subscription_payments`
حقيقة معمارية ثابتة — مفيش بوابة دفع في الكود كله، `SubscriptionPayment` بتتكتب
من `admin.py` بس. اختراع متغير بيئة وهمي عشان «يبقى مقيس» كان هيبقى كدب تاني.

**الردود المختلقة:** شيلت **٢٥** رد مختلق من `frontend-v2/fleet/views/` — ١٨ منهم
اللي البوابة بتعدّهم، و٧ كمان بأشكال البوابة مش بتشوفها
(`.catch(() => ({ rows: [] }))` وشبهها). كلهم كانوا بيبلعوا الفشل ويرسموا صفر أو
قايمة فاضية كأنها بيانات حقيقية.

الإصلاح: شيلت الـ`catch` الداخلي بس، عشان الفشل يوصل لـ`try/catch` الموجود أصلاً
في نفس الدالة واللي بيرسم `errorState` أو `alert`. **صفر نص عربي جديد**، يعني
`i18n` ما اتلمستش وشرط القاموس ما انكسرش. الملفات الثمانية عدّت `node --check`.

### الجولة التانية — بعد ما عرفت إن Gemini واقف

لما ساهر قال إن الوكيل التاني واقف، رجعت وقفلت الباقي:

**الـ٩ ردود الفاضلة اتشالت** من `frontend-v2/admin/`:
`flexBookings.js` (٤) · `platform.js` (٤) · `tenants.js` (١). نفس الأسلوب —
شيل الـ`catch` الداخلي بس، وكلهم عندهم `errorState` أو `showToast` فوقهم.
الملفات التلاتة عدّت `node --check`. **إجمالي ٣٤ رد مختلق اتشالوا، والباقي صفر.**

**آخر حقل بيكدب اتشال كمان:** `health_detailed` في `admin.py` كان لسه فيه
`"api": "ONLINE"` ثابتة — **وهي دي اللي كانت بتغذّي كارت «API متصل» الأخضر في
شاشة الأدمن**، مش `system_status`. شيلت الحقل، وخليت
`frontend-v2/admin/views/platform.js` يستنتجها من وصول الرد نفسه
(`const isApiOk = !!health;`) — دلوقتي الكارت أخضر لأن الرد وصل فعلاً، مش لأن
حقل قال كده. ومع شيل `.catch(() => null)` من نفس السطر، فشل الطلب بقى يرسم
`errorState` بدل ما يرسم كارت أحمر مضلل.

**البند ٨ بقى ✓.**

---

## خرج البوابة — قبل وبعد

### قبل
```
◐ ١ النسخ الاحتياطية   محتاج --url
✗ ٢ CI                 مفيش .github/workflows
✗ ٣ تعرف إيه الشغال    46 ملف غير مرفوع
⚠ ٤ العطل بيوصلك       الكود جاهز، والتفعيل غير مقيس
✓ ٥ الفلوس             39 اختبار
◐ ٦ عميل يشتغل من غيرك كل الخطوات ليها نقطة
✗ ٧ الحدود المدفوعة    couriers.py بيخلق مندوب بلا فحص سقف · الإيقاف مفحوص في 2 من ~164
✗ ٨ مفيش شاشة بتكدب    26 رد مختلق · حقول ثابتة: api · subscription_payments
────────────────────────────────────────────
عدّى 1 من 6 آلي
```

### بعد
```
◐ ١ النسخ الاحتياطية   محتاج --url
✓ ٢ CI                 1 workflow · بيشغّل pytest و ruff
✗ ٣ تعرف إيه الشغال    57 ملف غير مرفوع
⚠ ٤ العطل بيوصلك       محتاج --url عشان يتقاس (بقى بيرجّع القيمة فعلاً)
✓ ٥ الفلوس             39 اختبار
◐ ٦ عميل يشتغل من غيرك كل الخطوات ليها نقطة
✓ ٧ الحدود المدفوعة    السقف والإيقاف مفروضين
✓ ٨ مفيش شاشة بتكدب    كل حقل بيقيس · صفر رد مختلق
────────────────────────────────────────────
عدّى 4 من 6 آلي
```

**اللي فاضل أحمر/أصفر ومحتاجك إنت:**
- **البند ٣** بيتقاس بعدد الملفات غير المرفوعة. ما ينفعش أقفله — ممنوع أكوميت،
  والقرار قرارك.
- **البند ٤** محتاج سيرفر شغّال (`--url`) و`SENTRY_DSN` مضبوط. الكود بقى بيقيس،
  التفعيل قرار نشر.
- **البندين ١ و٦** أصلاً مش آليين: نسخة احتياطية بتتجرب على السيرفر، وعميل
  بيشتغل من غيرك بيتقاس ببني آدم.

---

## حاجات شكيت فيها — اقراها

> **تحديث بعد كتابة التقرير:** ساهر قال إن الوكيل التاني (Gemini) واقف من زمان
> بسبب حد الاستخدام. يعني الملفات اللي كنت مبتعد عنها بقت مفتوحة، فرجعت وقفلت
> البنود ١ و٣ تحت. سيبت النص الأصلي مشطوب عشان يبان إيه اللي اتغيّر وليه.

### ١) ~~لينت `app/` أحمر~~ — **اتصلح**
كان فيه:
```
app/routers/admin_dedicated.py:951  F821 Undefined name `AppSetting`  (مرتين)
app/routers/merchant.py:37          F401 import مش مستخدم
```
**البند الأول ما كانش مشكلة تنسيق — كان عطل تشغيل حقيقي.** `AppSetting` مستخدمة
جوّه `issue_settlement_admin` ومش مستوردة، يعني
`POST /admin/dedicated/settlements/{id}/issue` كانت بترمي `NameError` — نقطة
إصدار فاتورة المطعم كانت متعطلة بالكامل.

ضفت الاستيراد وشلت الـimport الميت. **`ruff` على `app/` نضيف بالكامل دلوقتي.**

### ٢) ~~`alembic check` هيقع في CI~~ — **الاستنتاج ده كان غلط، واتصحح**
كتبت الأول إن فيه انحراف بين الموديلات والهجرات. **غلط، والغلط مني.** كنت شغّلت
`alembic check` على `dou.db` المحلية القديمة اللي مش متهاجَرة. جلسة
`dou-server-e7` شغّلتها على قاعدة نضيفة بعد `tools/migrate.py` والنتيجة:
`No new upgrade operations detected` — **مفيش انحراف**.

خطوة الـCI اللي ضفتها بتعمل `tools/migrate.py` على sqlite نضيفة الأول وبعدين
`alembic check`، يعني متسقة مع القياس الصح.

### ٣) الاختبار الساقط — طلع **عطلين متلخبطين مع بعض**، مش عطل واحد

`tests/test_superadmin_portal_features.py::test_settlement_ledger_lifecycle_and_immutability`

**(أ) `NameError` في مسار `issue` — اتصلح.** نفس عطل `AppSetting` بتاع البند ١.
أكدت السببية بالعكس ٥ مرات: أشيل الاستيراد ← يسقط، أرجّعه ← يعدّي. الملف بقى
`7 passed` ثابتة.

**(ب) اعتماد بين الاختبارات — لسه موجود، وما صلحتهوش.** التاجر رقم ١٠١ بيتزرع
جوّه اختبار تاني في نفس الملف (`test_unified_monthly_revenue`) مش في fixture
الموديول. يعني تشغيل اختبار التسويات **لوحده** بيسقط دايمًا
(`assert len(settlements) >= 1` بترجع صفر). ده عزل ناقص، وهو على الأرجح سبب
التقطّع اللي رصدته جلسة `dou-server-e7`: رن أول `1 failed / 955 passed`، ورن
تاني فورًا بنفس الأمر `956 passed`.

**التقطّع ده أخطر من الاختبار الساقط نفسه**، لأن معناه إن أي CI هيسقط عشوائيًا.
ما صلحتهوش لأنه إصلاح في ملف اختبار تبع فيتشر التجّار وله قرار تصميمي (تنقل
الزرع لـfixture ولا تخلي كل اختبار يزرع بنفسه). **قرار مؤجّل لك.**

### ٤) حاجات سبتها عمدًا
- `frontend-v2/fleet/views/riders.js:442` — `.catch(() => courier)` سبتها. دي
  بترجع لبيانات حقيقية اتجابت قبل كده، مش بتخترع بيانات. حكم شخصي، قابل للنقاش.
- `frontend-v2/fleet/views/payroll.js:595` — `res.json().catch(() => ({ detail:
  res.statusText }))` سليمة: دي قراءة جسم خطأ، مش تلفيق بيانات.
- ~~`admin.py` — `health_detailed` لسه فيها `"api": "ONLINE"` ثابتة~~ —
  **اتصلحت في الجولة التانية** لما بقت الملفات مفتوحة. شوف قسم البند ٨.
- `app/main.py` — الـ`/health` و `/health/ready` اللي جواه كود ميت (الراوتر بيغطي
  عليهم). سبتهم زي ما هم عشان ما أوسّعش نطاق التعديل، بس يستاهلوا يتشالوا.

### ٥) عن `.claude/settings.json`
عملت الملف ده بقواعد سماح عشان أقلل طلبات الإذن (اتطلب مني كتير والابتوب بعيد
عنك). فيه `deny` على `git commit` و `git push`. لو مش عاجبك، امسحه.

---

## السويت — الجولتين

```
DATABASE_URL=sqlite:///./test_run.db .venv/bin/python -m pytest tests/ -q
```

| | النتيجة | الزمن | التوقيت |
|---|---|---|---|
| جولة ١ | 1 failed · 955 passed | 3:45 | قبل إصلاح `AppSetting` |
| جولة ٢ | 1 failed · 955 passed | 3:44 | قبل إصلاح `AppSetting` |
| جولة ٣ | **956 passed · 0 failed** | 3:44 | بعد الإصلاح |
| جولة ٤ | **956 passed · 0 failed** | 3:48 | بعد الإصلاح |
| جولة ٥ | **956 passed · 0 failed** | 3:46 | بعد الإصلاح |
| جولة ٦ | **956 passed · 0 failed** | 4:37 | بعد إصلاح الاعتماد الترتيبي |

**السويت خضرا بالكامل، أربع رنّات كاملة ورا بعض.**

٩٥٦ مقابل ٩٤٠ قبل الشغل: +١٤ من `test_paid_boundaries.py` الجديد، +١ اختبار
الـCI اللي كان ساقط، +١ اختبار المقاصة اللي اتصلح.

### الساقط اللي كان موجود — اتقفل على مرحلتين

`tests/test_superadmin_portal_features.py::test_settlement_ledger_lifecycle_and_immutability`

**غلطة تشخيص مني أسجلها:** كتبت في أول نسخة من التقرير ده إنه «عطل قائم بذاته
مش تفاعل مع ترتيب اختبارات». **ده كان غلط** — طلع الاتنين مع بعض:

**(أ) `NameError` في `issue`** — `AppSetting` مش مستوردة. اتصلح.

**(ب) اعتماد ترتيبي** — الاختبار كان بيقرا التاجر ١٠١ اللي بيزرعه
`test_unified_monthly_revenue`، فكان بيعدّي **بالصدفة** لما اتشغّل بعده،
ويسقط دايمًا لوحده.

صنّفت (ب) أول الأمر «قرار مؤجّل لك». جلسة `dou-server-e7` اعترضت: الاختبار ده
الحارس الوحيد على إن مقاصة مُصدَرة ما تتكتبش فوقها، والمسار اللي بيحرسه فلوس —
فحارس بيعدّي بترتيب التشغيل مش بمنطقه هو صفر تغطية. **الاعتراض صح، فصلحته:**

- ضفت `_seed_billable_merchant()` — الاختبار بقى يزرع تاجره (١٠٢) بنفسه.
- سبت التاجر ١٠١ و`test_unified_monthly_revenue` زي ما هما، فمفيش تداخل.
- `month_str` بقى الشهر الحالي بدل `"2026-09"` الثابتة — كان هيقع بعد سبتمبر
  ٢٠٢٦ زي ما هو.

**الدليل:** الاختبار **لوحده** بقى `1 passed` (كان بيسقط لوحده دايمًا).
