fastapi==0.140.13
starlette==1.3.1
httpx==0.28.1
requests==2.34.2
uvicorn==0.30.6
sqlalchemy==2.0.51
pydantic==2.9.2
python-dotenv==1.2.2
PyJWT==2.13.0
bcrypt==4.0.1
psycopg2-binary==2.9.10
alembic==1.19.1
# Backs app/services/cache.py and the shared login throttle. Without it both
# degrade silently to no-ops, so it is a real dependency, not an optional one.
redis==5.2.1
# Required by S3DocumentStorage and by scripts/backup.py when BACKUP_S3_BUCKET
# is set. Rider ID and passport scans live in this path, so it must not be a
# best-effort import that fails only once a document is uploaded.
boto3==1.35.76
# Error reporting. init_sentry() is a no-op without SENTRY_DSN, so this is safe
# to ship with reporting switched off.
sentry-sdk[fastapi]==2.19.2
# Writes the XLSX report exports.
openpyxl==3.1.5
